name: marketky_shop_backend
description: Dart REST API for the MarketKy-based shopping management database course project.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0

dependencies:
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^3.2.0
  dotenv: ^4.2.0
  mysql_client: ^0.0.27
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.2

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
